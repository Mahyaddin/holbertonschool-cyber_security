#!/usr/bin/env bash
# Manages manage_my_process script with total silence on background noise

case "$1" in
    start)
        ./manage_my_process > /dev/null 2>&1 &
        echo $! > /var/run/my_process.pid
        echo "manage_my_process started"
        ;;
    stop)
        # 1. Prosesi dayandir və xətanı gizlət
        pkill -f manage_my_process > /dev/null 2>&1
        # 2. PID faylını sil
        rm -f /var/run/my_process.pid
        # 3. YALNIZ tələb olunan mesajı çap et
        echo "manage_my_process stopped"
        ;;
    restart)
        pkill -f manage_my_process > /dev/null 2>&1
        rm -f /var/run/my_process.pid
        ./manage_my_process > /dev/null 2>&1 &
        echo $! > /var/run/my_process.pid
        echo "manage_my_process restarted"
        ;;
    *)
        echo "Usage: manage_my_process {start|stop|restart}"
        ;;
esac
