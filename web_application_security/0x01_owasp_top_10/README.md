# 0x01. OWASP Top 10 - Cryptographic Failures

This project focuses on understanding web application vulnerabilities, specifically dealing with cryptographic failures and decoding legacy encoding mechanism used in WebSphere application servers.

## Tasks
* **1-xor_decoder.sh**: A Bash script that decodes a WebSphere XOR-encoded string passed as the first argument (`$1`).
